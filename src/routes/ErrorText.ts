type ErrorText = {
    /** [plain] The header for the unknown route */
    header: string;
    /** [plain] The message for the unknown route */
    message: string;
};

export type { ErrorText as default };
