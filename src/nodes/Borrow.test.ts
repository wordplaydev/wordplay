import { testConflict } from "../conflicts/TestUtilities";
import Borrow from "./Borrow";

test("Test borrow conflicts", () => {

    // testConflict('↓ moose', '↓ mouse', Borrow, UnknownBorrow);

});