<!-- The debug mode mark: two bare pause bars, matching PlayIcon's bare
     triangle in weight and extent so the switcher's marks read as one set.
     Drawn rather than ⏸ because the playback codepoints render unpredictably
     across platforms (emoji vs. text presentation, system vs. bundled
     fonts). Decorative: the control carrying it holds the accessible
     label. -->
<svg
    width="1em"
    height="1em"
    viewBox="0 0 14 14"
    style="fill: currentColor; vertical-align: -0.125em; pointer-events: none;"
    aria-hidden="true"
    focusable="false"
    ><path
        d="M3.25,1.75 h2.75 v10.5 h-2.75 Z M8,1.75 h2.75 v10.5 h-2.75 Z"
    /></svg
>
