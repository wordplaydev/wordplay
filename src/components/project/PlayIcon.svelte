<!-- The play mode mark: a bare right-pointing triangle, no background,
     matching DebugIcon's bare bars. Drawn rather than ▶ because that
     codepoint renders unpredictably across platforms (emoji vs. text
     presentation). Decorative: the control carrying it holds the accessible
     label. -->
<svg
    width="1em"
    height="1em"
    viewBox="0 0 14 14"
    style="fill: currentColor; vertical-align: -0.125em; pointer-events: none;"
    aria-hidden="true"
    focusable="false"
    ><path d="M3.5,1.75 L12.25,7 L3.5,12.25 Z" /></svg
>
