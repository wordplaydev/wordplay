enum Purpose {
    STORE = 'store',
    COMPUTE = 'compute',
    INPUT = 'input',
    OUTPUT = 'output',
    DECIDE = 'decide',
    CONVERT = 'convert',
    PROJECT = 'project',
}

export default Purpose;
