name: Verify PR
on:
    pull_request:
        branches: ['main']
jobs:
    unit:
        uses: ./.github/workflows/unit.yml

    playwright:
        uses: ./.github/workflows/playwright.yml
        secrets: inherit
