---
description: Draft a CHANGELOG.md entry for the current change — emoji + 6th-grade text + production-URL link + issue/PR citation. Asks before writing.
---

Follow the workflow defined in [.claude/skills/changelog/SKILL.md](../skills/changelog/SKILL.md). Treat it as the source of truth for emoji selection, text style, URL linking, and the verification steps; this file just registers the slash command in environments (like the VS Code extension) that only pick up `.claude/commands/`.

Start by reading that file in full, then proceed through its workflow against the current uncommitted changes.
