🎹
虚拟钢琴
=== start
¶一个可以弹奏的单八度钢琴！为了获得最佳体验，请关闭音乐可视化。¶

¶这个绑定保存了一个 Key 流，用来检测你按下的是哪个键。¶
按下2: 钥匙()

¶这里保存了一个映射，把你按下的键对应到它们要弹奏的音符，包含文本形式（用来显示正在弹奏的音符）和数字文本字面量（用来转换成数字（#）并输入到 Track 列表中）。

这里选用的特定布局尽量模拟钢琴键盘，白键用字母字符表示，黑键用数字字符表示。¶
音符表: {
		'w': ['C' "1"]
		'3': ['C# / Db' "2"]
		'e': ['D' "3"]
		'4': ['D# / Eb' "4"]
		'r': ['E' "5"]
		't': ['F' "6"]
		'6': ['F# / Gb' "7"]
		'y': ['G' "8"]
		'7': ['G# / Ab' "9"]
		'u': ['A' "10"]
		'8': ['A# / Bb' "11"]
		'i': ['B' "12"]
		'o': ['C5' "13"]
	}

¶这个函数会根据按下的键，返回对应音符的名字。如果按下的键不是有效的音符，就返回空文本。¶
ƒ 返回Text(键2•"")•"" (音符表{键2} ≠ ø) ? 音符表{键2}[1] ""

¶这个函数会返回按下的键在列表中对应的音符编号，用作 Track 的输入。如果按下的键无效，就返回一个空列表。¶
ƒ 返回Note(键3•"")•[] [
		(音符表{键3} ≠ ø) ? (音符表{键3}[2] → #) []
	]

¶这个函数接收一个数字列表，返回一个带有该音符列表的 Track。循环是关闭的，这样按下键时音符只会播放一次。¶
ƒ 返回Track(音符2•[#]) 音轨(音符2 循环: ⊥)

¶这两个绑定定义了对应键被按下时和没被按下时要显示的颜色。¶
按下Color: 颜色(10 270 50°)
空Color: 颜色(0 0 0°)

¶这会播放按下的键所对应的音轨。音阶设置为半音阶，这样音轨就能用到所有半音。重播设置为在按下有效键时触发，所以就算连续两次按下同一个键，音轨也会每次都播放。¶
音乐(
	返回Track(返回Note(按下2))
	音阶: 音乐.半音阶
	重播: 音符表{按下2} ≠ ø
)

¶显示钢琴、要按的键，以及按键时弹奏出的音符。¶
组合(
	堆叠('>' 1m)
	[
		组合(
			横排('>' -.3m)
			[
				短语(
					'▯'
					名字: 'w'
					尺寸: 3m
					颜色: 按下2 = 'w' ? 按下Color 空Color
				)
				短语(
					'▮'
					名字: '3'
					尺寸: 3m
					颜色: 按下2 = '3' ? 按下Color 空Color
				)
				短语(
					'▯'
					名字: 'e'
					尺寸: 3m
					颜色: 按下2 = 'e' ? 按下Color 空Color
				)
				短语(
					'▮'
					名字: '4'
					尺寸: 3m
					颜色: 按下2 = '4' ? 按下Color 空Color
				)
				短语(
					'▯'
					名字: 'r'
					尺寸: 3m
					颜色: 按下2 = 'r' ? 按下Color 空Color
				)
				短语(
					'▯'
					名字: 't'
					尺寸: 3m
					颜色: 按下2 = 't' ? 按下Color 空Color
				)
				短语(
					'▮'
					名字: '6'
					尺寸: 3m
					颜色: 按下2 = '6' ? 按下Color 空Color
				)
				短语(
					'▯'
					名字: 'y'
					尺寸: 3m
					颜色: 按下2 = 'y' ? 按下Color 空Color
				)
				短语(
					'▮'
					名字: '7'
					尺寸: 3m
					颜色: 按下2 = '7' ? 按下Color 空Color
				)
				短语(
					'▯'
					名字: 'u'
					尺寸: 3m
					颜色: 按下2 = 'u' ? 按下Color 空Color
				)
				短语(
					'▮'
					名字: '8'
					尺寸: 3m
					颜色: 按下2 = '8' ? 按下Color 空Color
				)
				短语(
					'▯'
					名字: 'i'
					尺寸: 3m
					颜色: 按下2 = 'i' ? 按下Color 空Color
				)
				短语(
					'▯'
					名字: 'o'
					尺寸: 3m
					颜色: 按下2 = 'o' ? 按下Color 空Color
				)
			]
		)

		组合(
			横排('>' 1.1m)
			[
				短语('w')
				短语('3')
				短语('e')
				短语('4')
				短语('r t')
				短语('6')
				短语('y')
				短语('7')
				短语('u')
				短语('8')
				短语('i  o')
			]
		)
		短语(返回Text(按下2))
	]
)
